I"Ó(function() {
  wiki.Likes = {
    updateLike: function(likeable, id, liked) {
      if (liked == null) {
        liked = true;
      }
      if (liked) {
        return $("#like-for-" + likeable + "-" + id).addClass('liked').data('method', 'delete');
      } else {
        return $("#like-for-" + likeable + "-" + id).removeClass('liked').data('method', 'post');
      }
    },
    updateLikes: function(likeable, id, liked) {
      var i, len, results;
      if (liked == null) {
        liked = true;
      }
      results = [];
      for (i = 0, len = ids.length; i < len; i++) {
        id = ids[i];
        results.push(this.updateLike(likeable, id, liked));
      }
      return results;
    }
  };

}).call(this);
:ET