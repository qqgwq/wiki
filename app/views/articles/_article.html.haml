- cache @article do
  #index1
    %h2= link_to article.title, article
    %p= article.user&.name
    =image_tag article.user.image if article.user

    %h2
    = article.comments.count
    = t('.Comments')
    %span= time_ago_in_words(article.created_at)
  -#.pull-right
    -#= link_to article_like_path(article), method: :post, remote: true do
      -#%i.fa.fa-heart
  -#.span-count1
    -#= article.likes_count